---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { ViewTransitions } from "astro:transitions";
import { SITE_TITLE, SITE_DESCRIPTION, TRANSITION_API } from "../config";

const {
  image,
  title = SITE_TITLE,
  description = SITE_DESCRIPTION,
  ogType,
} = Astro.props;
---

<!doctype html>
<html lang="en" data-theme="lofi">
<head>
    <BaseHead title={title} description={description} image={image} ogType={ogType} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg?v=2" />
    {TRANSITION_API && <ViewTransitions />}
  </head>
  <body>
    <div class="bg-[#f4f4f4] min-h-screen">
      <Header title={SITE_TITLE} />
      
      <div class="flex justify-center w-full">
        <main class="p-6 pt-64 w-full max-w-[1200px]">
          <slot />
        </main>
      </div>
      <Footer />
    </div>

    <div id="custom-cursor"></div>

    <script is:inline>
      function initCursor() {
        const cursor = document.getElementById('custom-cursor');
        if (!cursor) return;

        let mouseX = 0;
        let mouseY = 0;

        const onMouseMove = (e) => {
          mouseX = e.clientX;
          mouseY = e.clientY;
          
          requestAnimationFrame(() => {
            cursor.style.left = mouseX + 'px';
            cursor.style.top = mouseY + 'px';
            cursor.style.opacity = "1";
          });
        };

        const onMouseDown = () => cursor.classList.add('cursor-clicking');
        const onMouseUp = () => cursor.classList.remove('cursor-clicking');

        window.addEventListener('mousemove', onMouseMove);
        window.addEventListener('mousedown', onMouseDown);
        window.addEventListener('mouseup', onMouseUp);

        // Feedback para links e botões
        document.addEventListener('mouseover', (e) => {
          if (e.target.closest('a') || e.target.closest('button')) {
            cursor.style.transform = 'translate(-50%, -50%) scale(2.5)';
            cursor.style.backgroundColor = 'rgba(27, 86, 253, 0.4)';
          }
        });

        document.addEventListener('mouseout', (e) => {
          if (e.target.closest('a') || e.target.closest('button')) {
            cursor.style.transform = 'translate(-50%, -50%) scale(1)';
            cursor.style.backgroundColor = 'rgb(1, 24, 216)';
          }
        });
      }

      initCursor();
      document.addEventListener('astro:after-swap', initCursor);
      
      // Função global de fechar modal movida para escopo window real
      window.closeModal = function(el) {
        const d = el.closest('dialog');
        if (d) d.close();
      };
    </script>

<style is:global>

  /* 1. RESET E CORES GERAIS */
  html, body {
    background-color: #f4f4f4 !important;
    color: rgb(27, 86, 253); /* Define a cor padrão sem travar tudo */
    margin: 0;
    padding: 0;
    cursor: none !important;
  }

  /* Esconde o mouse original mas permite cores customizadas */
  * { 
    cursor: none !important; 
    box-sizing: border-box; 
  }

  /* 2. CONTROLE DO HEADER E MAIN */
  header {
    padding-top: 20px; 
    padding-bottom: 10px;
    min-height: auto !important; 
    margin-bottom: 0 !important;
  }

  main {
    margin-top: 10px !important; 
    padding-top: 90px !important; 
  }

  .nav-wrapper, .header-container {
    align-items: flex-start !important;
  }

  /* 3. EXCEÇÕES E ELEMENTOS ESPECÍFICOS */
  .blue-overlay {
    background-color: rgba(27, 86, 253, 0.95) !important;
  }

  .blue-overlay *, .play-text, .project-title-overlay {
    color: white !important;
  }

  /* 4. O CURSOR CUSTOMIZADO */
  #custom-cursor {
    width: 25px;
    height: 25px;
    background-color: rgb(1, 24, 216) !important; 
    border-radius: 50%;
    position: fixed;
    top: 0;
    left: 0;
    pointer-events: none;
    z-index: 100000; 
    filter: blur(4px);
    transform: translate(-50%, -50%);
    transition: transform 0.2s ease, background-color 0.2s ease, opacity 0.3s ease;
    opacity: 0;
  }

  /* 5. SELEÇÃO E VÍDEOS */
  ::selection {
    background: rgb(27, 86, 253);
    color: white;
  }

  .video-placeholder {
    position: relative;
    overflow: hidden;
  }

  .video-placeholder iframe {
    width: 100% !important;
    height: 100% !important;
    position: absolute;
    top: 0;
    left: 0;
    border: none;
  }

</style>
  </body>
</html>